// Configuración de Firebase
// Nota: Los valores de configuración deben venir del archivo google-services.json (Android) 
// y GoogleService-Info.plist (iOS) que descargas de la consola de Firebase

import firebase from '@react-native-firebase/app';

// La configuración se maneja automáticamente por los archivos de configuración nativos
// No necesitas configurar manualmente aquí si tienes los archivos correctos

export default firebase;
